# 更新日志

本文件记录 AirCOM 各版本的变化。版本号规则：主.次.修订（语义化版本）。

- 主版本：架构性大变化（里程碑完成）
- 次版本：新功能 / 里程碑内重要更新
- 修订：bug 修复 / 小调整

## 里程碑

| 标签 | 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|---|
| `lan-v1` | v0.1.4 | 2026-08-12 | ✅ 完成 | 局域网版本：单程序双角色，com0com 自动管理，两台电脑同局域网串口远程映射 |

---

## v0.1.7 - 2026-08-12

修复连接状态下关闭程序卡死、对端停止后本端检测不到的问题。

### 修复

- **修复 UI 线程死锁**：v0.1.6 在窗口关闭/返回角色时用 `.GetAwaiter().GetResult()` 在 UI 线程同步等异步 Dispose，而死锁了。改为全异步（fire-and-forget Dispose，Back 命令改 async），并加 `_disposing` 标志防止 `OnStopped` 在主动关闭时重入。
- **修复 catch 块里的同步等**：StartASide/StartBSide 失败后的 Dispose 也改为 await。
- 确保 B 端停止/关闭时正常 Dispose -> TCP 发 FIN -> A 端秒级检测到断开（不再依赖 25 秒 keep-alive）。

### 影响

- 连接状态下关闭程序不再卡死。
- B 端停止后，A 端几秒内检测到并显示"已断开（可重新连接）"。

---

## v0.1.6 - 2026-08-12

修复"对端关闭后本端仍显示已连接"的 bug。

### 修复

- **窗口关闭时正确释放连接**：WorkWindow 关闭时调用 ViewModel.Dispose，让 EngineHost 正常释放，TCP 连接发送 FIN 包。之前直接点窗口 X 关闭程序时，BEngineHost 没有正常释放，TCP 不发 FIN，A 端无法检测到断开。
- **TCP keep-alive**：两端 TCP 连接启用 keep-alive（10 秒空闲后开始探测，每 3 秒一次，5 次失败约 25 秒判定断开）。这样即使对端崩溃、断电、拔网线等不发 FIN 的场景，本端也能检测到死连接并触发"已断开"。

### 影响

- 对端关闭、崩溃、网络中断后，本端在约 25 秒内自动检测到并重置为可重连状态。

---

## v0.1.5 - 2026-08-12

打磨局域网版本到产品级。

### 新增

- **配置持久化**：A 端记住上次填的 B 端 IP，B 端记住上次选的串口，两端记住上次的端口和波特率。启动时自动加载，下次不用重填。（A/B 共用 `%AppData%\AirCOM\aircom-settings.json`）
- **友好错误提示**：串口被占用、监听端口被占用、连不上 B 端等常见错误，给出清晰中文提示和排查建议，不再显示原始异常堆栈。
- **断开/重连健壮性**：对端先退或网络断开时，UI 自动重置为"已断开（可重新连接）"，清理 host 资源，用户可直接重新连接，不会卡在运行状态。

### 影响

- 日常使用体验显著提升：开机即用（配置已存）、出错能看懂、断线能恢复。

---

## v0.1.4 - 2026-08-12

修复虚拟口对反复分配、号一直增长的 bug。

### 修复

- **settings 存放位置改为 %AppData%\AirCOM**：之前 settings.json 存在 exe 同目录，导致从不同版本目录（v0.1.0/v0.1.1/...）启动时各自读自己的 settings，每次都判定"未分配"而新建口对，造成 COM50/51、52/53、54/55... 一路增长且全部残留。改为用户级 AppData 后，所有版本共享同一份配置，口对分配稳定。
- 新增 `tools/com0com/cleanup-ports.ps1`：清理多余 com0com 口对的工具脚本（管理员运行，交互式选择保留哪对，删除其余）。

### 影响

- 以后无论从哪个解压目录启动 AirCOM，虚拟口对都固定不变（首次分配后存入 AppData，长期复用）。

---

## v0.1.3 - 2026-08-12

重大改进：A 端首次使用无需重启、无测试模式水印。

### 重大发现 / 修复

- **com0com 不需要测试签名**：经 B 电脑干净机器反测验证，随包的 com0com 驱动（`com0com-setup-x64.exe`，签名方 CyberCircuits，COMODO 商业代码签名）在 64 位 Win10/11 可直接加载，无需 `bcdedit /set testsigning on`、无需重启、无桌面水印。修正了之前"com0com 必须开测试签名"的错误认知。
- **移除测试签名相关逻辑**：`com0com-repair.ps1` 不再执行 `bcdedit`，不再提示重启；只保留驱动签名状态信息性检查。WorkViewModel 弹窗文案同步更新。
- com0com 服务设为 `Automatic`（详见 v0.1.2，本版本一并包含）。

### 影响

- A 端首次使用流程大幅简化：解压 -> 运行 -> 选 A -> 连接 -> UAC 是 -> 驱动安装 -> 即用。无重启、无水印。

---

## v0.1.2 - 2026-08-12

小修版本（仅发布，无独立条目，改动已并入 v0.1.3）。

- com0com 服务创建时设为 `Automatic`（开机自启），避免口对偶发消失。

---

## v0.1.1 - 2026-08-12

M5 验证中发现的修复版本。

### 修复

- 修复 `com0com-repair.ps1` 中函数参数名 `$args` 与 PowerShell 自动变量冲突的问题（改为 `$SetupArgs`）。
- 修复 `Start-Process` 的 `RedirectStandardOutput` 与 `RedirectStandardError` 不能指向同一文件的问题（拆分为两个临时文件）。

### 验证

- 单程序 A/B 双角色真机联调通过，两台电脑串口助手双向收发数据正常。

---

## v0.1.0 - 2026-08-12

首个绿色单程序版本。合并 A/B 双角色，com0com 自动管理，自动选端口。

### 新增

- **单程序双角色**：一个 `AirCOM.exe`，启动选"使用远程串口(A端)"或"共享本地串口(B端)"，不再分两个 exe。
- **com0com 自动管理**：A 端启动时自动检测驱动服务/虚拟口对状态，未就绪时弹窗引导，UAC 提权自动跑修复脚本（装驱动、建口对、开测试签名）。
- **自动选空闲端口**：A 端首次用自动从 COM50 起找一对空闲 COM 号（如 COM50↔COM51），避开与物理端口或其他虚拟口软件冲突。
- **端口分配持久化**：自动选好的端口对写入 `aircom-settings.json`，下次开机沿用，串口软件可固定开同一个 COM 号。
- **绿色免安装包**：解压即用，自带 VC++ 运行时和 com0com 安装包，B 端真零安装，A 端首次需一次提权+重启。
- 版本号显示在角色选择界面标题。

### 修复

- 修复 `app.manifest` 根元素格式错误导致目标机器启动报"并行配置不正确"(SxS) 的问题。
- 修复发布包 native 库嵌入导致部分机器无法加载的问题（改为 native 库外置）。

### 已验证

- 全解决方案 0 警告 0 错误，40 项单元测试通过。
- 两台电脑真机双向联调通过（B 端 USB-TTL，A 端串口软件互传数据）。

---

## v0.0.x - 2026-08-10/11 (开发阶段，未版本化)

- M0：协议帧编解码（12 种消息）、串口抽象层、传输层（TcpTransport + FramedConnection）。
- M1：SerialBridge 双向泵、ControlLinePoller、A/B EngineHost、局域网直连打通。
- 单机回环端到端测试通过；两机真机双向联调通过。
- com0com 驱动安装流程跑通（含测试签名、pnputil、sc create、setupc install）。
