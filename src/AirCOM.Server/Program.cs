namespace AirCOM.Server;

internal static class Program
{
    private static async Task Main(string[] args)
    {
        Console.WriteLine("AirCOM Server (信令 + 备选中继)");
        Console.WriteLine("开发中 - M2 阶段实现");
        await Task.Delay(Timeout.Infinite);
    }
}
